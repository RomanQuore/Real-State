const scrollDownButton = document.querySelector('#scrollDownButton');

scrollDownButton.addEventListener('click', () => {
  const scrollDownOptions = document.querySelector('#scrollDownOptions');
  scrollDownOptions.style.display = 'block'; /* Show the options on click */
});